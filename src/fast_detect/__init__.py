"""
fast_detect — YOLO-based object detection for batch video files.

Scans one or more videos (including DVR .dav files), detects objects of
interest, and exports a JSON report with the timestamps of every detection.
"""

__version__ = "0.1.0"
